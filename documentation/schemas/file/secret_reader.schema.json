{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/benedeki/dbd/documentation/schemas/file/secret_reader.schema.json",
  "title": "File SecretReader Config",
  "description": "Config node for a filesystem-backed SecretReader.\n\nPoints at a directory of secret files and selects which of them\nare exposed via get_secrets_list()/get_secret().",
  "type": "object",
  "properties": {
    "file_name": {
      "title": "File Name",
      "description": "Name of the secret file. Must exist at validation time.",
      "type": "string",
    },
    "type": {
      "title": "Type",
      "description": "Type of the source provider.",
      "type": "string",
      "default": "json",
      "enum": ["json"]
    }
  },
  "required": ["file_name"]
}
