{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/benedeki/dbd/documentation/schemas/aws/secret_reader.schema.json",
  "title": "AWS SecretReader Config",
  "description": "Config node for an AWS Secrets Manager-backed SecretReader.\n\nPoints at a directory of secret files and selects which of them\nare exposed via get_secrets_list()/get_secret().",
  "type": "object",
  "properties": {
    "region_name": {
      "title": "Region Name",
      "description": "AWS region name where the secrets are stored.",
      "type": "string"
    }
  },
  "required": ["region_name"]
}
